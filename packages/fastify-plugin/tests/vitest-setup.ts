/**
 * Vitest setup for Fastify plugin tests
 * Configure TypeScript and module resolution
 */

// Set up environment for tests
process.env.NODE_ENV = 'test';

// Import the error suppression setup
import './setup.js';
