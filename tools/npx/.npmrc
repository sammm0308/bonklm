# npm provenance configuration for Sigstore attestation
# Provenance requires CI/CD environment (GitHub Actions, GitLab CI, etc.)
# Enable in CI: npm publish --provenance
# See: https://docs.npmjs.com/generating-provenance-statements
# provenance=true
